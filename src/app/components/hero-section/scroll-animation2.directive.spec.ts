import { ScrollAnimation2Directive } from './scroll-animation2.directive';

describe('ScrollAnimation2Directive', () => {
  it('should create an instance', () => {
    const directive = new ScrollAnimation2Directive();
    expect(directive).toBeTruthy();
  });
});
