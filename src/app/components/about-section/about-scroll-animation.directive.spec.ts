import { AboutScrollAnimationDirective } from './about-scroll-animation.directive';

describe('AboutScrollAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new AboutScrollAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
